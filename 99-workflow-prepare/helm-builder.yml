# name: publish helm-builder

# on:
#   push:
#     paths:
#       - 'helm-builder/version.txt'
#       - '.github/workflows/helm-builder.yml'

# jobs:
#   build_image:
#     runs-on: ubuntu-latest

#     env:
#       DOCKER_REPO: denisitpro/helm-builder
#       DOCKER_FILE: Dockerfile

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4

#       - name: Extract version
#         id: vars
#         run: |
#           cd helm-builder
#           echo "VERSION=$(cat version.txt)"
#           echo "VERSION=$(cat version.txt)" >> $GITHUB_ENV
#           echo "TIMESTAMP=$(date -u +%Y%m%d%H%M%S)" >> $GITHUB_ENV

#       - name: Load app versions
#         run: |
#           cd helm-builder
#           while IFS='=' read -r key value; do
#             echo "$key=$value" >> $GITHUB_ENV
#           done < app-version.txt

#       - name: Login to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Set up Docker Buildx
#         id: create_builder
#         uses: docker/setup-buildx-action@v3

#       - name: Build and push
#         uses: docker/build-push-action@v6
#         with:
#           context: ./helm-builder
#           file: ./helm-builder/${{ env.DOCKER_FILE }}
#           push: true
#           platforms: linux/amd64,linux/arm64
#           tags: |
#             ${{ env.DOCKER_REPO }}:${{ env.VERSION }}
#             ${{ env.DOCKER_REPO }}:latest
#           build-args: |
#             TIMESTAMP=${{ env.TIMESTAMP }}
#             VERSION=${{ env.VERSION }}
#             ALPINE_VERSION=${{ env.ALPINE_VERSION }}
#             HELM_VERSION=${{ env.HELM_VERSION }}
#             YQ_VERSION=${{ env.YQ_VERSION }}
#           builder: ${{ steps.create_builder.outputs.name }}
